{
  "name": "ts-lib1",
  "description": "top-level dependent lib",
  "version": "0.0.1",
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "scripts": {
    "postinstall": "typings install && tsc"
  },
  "dependencies": {
    "typescript": "git+ssh://github.com/unitoio/TypeScriptPatched.git",
    "typings": "^0.5.1"
  }
}
